<?php

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $alice = new App\User();
        // $alice->name = 'Alice';
        // $alice->email = 'alice@gmail.com';
        // $alice->password = bcrypt('12345');
        // $alice->save();
        // $this->call(UsersTableSeeder::class);
    }
}
